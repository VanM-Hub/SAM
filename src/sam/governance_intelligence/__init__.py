"""SAM 3.1 — Governance Intelligence (IP-3.1-001).

A layered Intelligence capability added above the existing SAM 2.0 platform.
Per the Implementation Strategy, implementation starts from Foundation
Knowledge (WP-01) and proceeds toward Integration (WP-14) — NOT from UI or
LLM.

The package deliberately uses plain deterministic data structures and simple
routines. It must stay OFF the LLM/UI path (no AI, no vectors) until the
Engineer's Evidence Index (WP-04) proves the knowledge base is resolvable.
"""

__version__ = "0.1.0"
